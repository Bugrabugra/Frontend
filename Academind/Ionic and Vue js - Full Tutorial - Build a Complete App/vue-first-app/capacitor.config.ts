import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'bugra.google.com',
  appName: 'Vue Memories',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
